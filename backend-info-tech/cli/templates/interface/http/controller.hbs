import { Request, Response } from "express"; import {
{{pascalCase name}}Repository } from "../../infra/repository/{{pascalCase
  name
}}Repository"; import { Create{{pascalCase name}}
} from "../../application/usecases/Create{{pascalCase name}}"; export class
{{pascalCase name}}Controller { async create(req: Request, res: Response) { try
{ const repo = new
{{pascalCase name}}Repository(); const usecase = new Create{{pascalCase
  name
}}(repo); await usecase.execute(req.body); return res.status(201).json({
message: "{{pascalCase name}}
criado com sucesso" }); } catch (error) { return res.status(500).json({ error:
"Erro ao criar
{{camelCase name}}" }); } } }