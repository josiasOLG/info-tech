import { Router } from "express"; import {
{{pascalCase name}}Controller } from "./{{pascalCase name}}Controller"; const
router = Router(); const controller = new
{{pascalCase name}}Controller(); router.post("/",
controller.create.bind(controller)); export { router as
{{camelCase name}}Routes };