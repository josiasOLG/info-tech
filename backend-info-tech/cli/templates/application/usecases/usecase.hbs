import { Create{{pascalCase name}}DTO } from "../dto/Create{{pascalCase name}}DTO";
import { I{{pascalCase name}}Repository } from "../../domain/I{{pascalCase name}}Repository";
import { {{pascalCase name}} } from "../../domain/{{pascalCase name}}";

export class Create{{pascalCase name}} {
  constructor(private repo: I{{pascalCase name}}Repository) {}

  async execute(data: Create{{pascalCase name}}DTO): Promise<void> {
    const entity = new {{pascalCase name}}(data);
    await this.repo.create(entity);
  }
}
