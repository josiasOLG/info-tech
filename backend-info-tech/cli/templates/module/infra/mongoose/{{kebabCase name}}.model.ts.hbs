import { Schema, Document } from 'mongoose';

export interface {{pascalCase name}}Document extends Document {
{{#each fieldsArray}}
  {{this.key}}: {{this.type}};
{{/each}}
}

export const {{camelCase name}}Schema = new Schema({
{{#each fieldsArray}}
  {{this.key}}: {
    type: {{#eq this.type "string"}}String{{else if (eq this.type "number")}}Number{{else if (eq this.type "boolean")}}Boolean{{else}}Schema.Types.Mixed{{/eq}},
    required: true
  },
{{/each}}
});
