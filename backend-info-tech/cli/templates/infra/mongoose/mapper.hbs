import {
{{pascalCase name}}
} from "../../domain/{{pascalCase name}}"; import {
{{pascalCase name}}ModelClass } from "./{{pascalCase name}}Model"; export class
{{pascalCase name}}Mapper { static toDomain(raw:
{{pascalCase name}}ModelClass):
{{pascalCase name}}
{ return new
{{pascalCase name}}( {
{{#each fieldsArray}}
  {{this.key}}: raw.{{this.key}},
{{/each}}
}, raw._id.toString() ); } static toPersistence(entity:
{{pascalCase name}}) { return {
{{#each fieldsArray}}
  {{this.key}}: entity.props.{{this.key}},
{{/each}}
}; } }