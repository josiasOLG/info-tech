import { prop, getModelForClass } from "@typegoose/typegoose"; export class
{{pascalCase name}}ModelClass {
{{#each fieldsArray}}
  @prop({ required: true{{#if (eq this.key "plate")}}, unique: true{{/if}}
  })
  {{this.key}}!:
  {{this.type}};
{{/each}}
} export const
{{pascalCase name}}Model = getModelForClass({{pascalCase name}}ModelClass, {
schemaOptions: { collection: "{{camelCase name}}s" }, });